apiVersion: v1
kind: ConfigMap
metadata:
  name: notifications-service-config
data:
  RABBITMQ_HOST: "rabbitmq-service"
  RABBITMQ_USER: "user"
  RABBITMQ_EXCHANGE: "coniiti_events"
  RABBITMQ_QUEUE: "notifications_queue_v2"
  RABBITMQ_DLX: "coniiti_events_dlx"
  RABBITMQ_DLQ: "notifications_dead_letter"
  AUTH_SERVICE_URL: "http://auth-service"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-service
  labels:
    app: notifications-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications-service
  template:
    metadata:
      labels:
        app: notifications-service
    spec:
      containers:
      - name: notifications-service
        image: notifications-service:latest
        imagePullPolicy: IfNotPresent
        command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
        resources:
          requests:
            memory: "64Mi"
            cpu: "40m"
          limits:
            memory: "176Mi"
            cpu: "250m"
        ports:
        - containerPort: 8000
        envFrom:
        - configMapRef:
            name: notifications-service-config
        - secretRef:
            name: notifications-service-secret
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 20
          periodSeconds: 15
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 8
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: notifications-service
spec:
  type: ClusterIP
  selector:
    app: notifications-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
